{
  "name": "promise-front",
  "private": true,
  "scripts": {
    "link": "lerna link",
    "clean": "lerna clean -y",
    "bootstrap": "lerna bootstrap",
    "components": "lerna run storybook --stream",
    "app": "lerna run start --scope=@promise-front/app --stream"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.4.1"
  },
  "version": "0.0.0"
}
